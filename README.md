# hello-greg

Hello Greg (built by software factory from LIN-49).

Single-page Next.js app: greets Greg and shows the current date and time once per page load (viewer locale).

## Development

```bash
npm install
npm run dev
```

Open [http://localhost:3000](http://localhost:3000).

## Build

```bash
npm run build
```
